spring:
  datasource:
    driver-class-name:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql
    properties:
      hibernate:
        format_sql: true

  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    port: 587
    username: tarikbelaid12334@gmail.com
    password: hzku nwpx xbjt cpqf
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true